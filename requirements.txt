Flask
requests
python-dotenv
# Python has no system IANA timezone database on Windows, which is where GREMLIN
# is deployed. Without this, zoneinfo cannot load the plant timezone and monthly
# availability would silently bucket work orders in UTC.
tzdata
